{% set name = "libertem-live" %}
{% set version = "0.3.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name | replace('-', '_') }}-{{ version }}.tar.gz
  sha256: b185289b2652bb14e963e9b818d290c8a06f1d08cf6b867bc6890acf38d83715

build:
  skip: true  # [py<37]
  entry_points:
    - libertem-live-mib-sim=libertem_live.detectors.merlin.sim:main
    - libertem-live-dectris-sim=libertem_live.detectors.dectris.sim:main
    - libertem-live-asi-tpx-sim=libertem_live.detectors.asi_tpx3.sim:main
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - python
    - pip
  run:
    - python
    - libertem >=0.11.0
    - numpy
    - click
    - tqdm
    - numba
    - opentelemetry-api
    - sparseconverter
    - libertem-dectris
    - libertem-asi-tpx3

test:
  imports:
    - libertem_live
  commands:
    - pip check
    - libertem-live-mib-sim --help
    - libertem-live-dectris-sim --help
    - libertem-live-asi-tpx-sim --help
    - pytest tests
  source_files:
    - tests
    - conftest.py
    - pytest.ini
  requires:
    - pip
    - flask
    - pytest >=6
    - pytest-cov
    - pytest-asyncio >0.11
    - pytest-xdist
    - pytest-env
    - pytest-benchmark
    - pytest-timeout
    - aiohttp
    - websockets
    - ipykernel
    - nest-asyncio

about:
  home: https://libertem.github.io/LiberTEM-live
  summary: Live processing with LiberTEM
  license: GPL-3.0-or-later AND MIT
  license_file:
    - LICENSE
    - src/libertem_live/detectors/LICENSE
  description: |
    LiberTEM-live is an extension module for
    LiberTEM that allows live data processing.

extra:
  recipe-maintainers:
    - sk1p
    - uellue
    - matbryan52
